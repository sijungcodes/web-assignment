import React from 'react';

const Book = (props) => {
    return (
            <div className=" ">
                <div className=" ">{props.title}</div>
            </div>
    );  
};

export default Book;